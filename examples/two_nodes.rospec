node type talker_type {
    publishes to /chatter: std_msgs/msg/String;
}

node type listener_type {
    subscribes to /chatter: std_msgs/msg/String;
}

system {
    node instance talker: talker_type {
    }

    node instance listener: listener_type {
    }
}
[project]
name = "rosqa"
version = "0.1.0"
description = "Generate comprehension questions from ROS2 rospec specifications"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
rosqa = "rosqa.cli:main"

[tool.setuptools.packages.find]
where = ["src"]